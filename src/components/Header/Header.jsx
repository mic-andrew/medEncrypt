
import './header.css'; 

const Header = () => {
    return (
        <header className="header">
            <div className="logo">
                <span className="logo-text">MedCrypt</span>
            </div>
            <nav className="nav-menu">
                {/* Add navigation links here if needed */}
            </nav>
        </header>
    );
};

export default Header;
